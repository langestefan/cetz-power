---
title: Radial feeder with step-down transformer
description: Textbook layout — grid → transformer → bus → load.
---

import Snippet from '../../../components/Snippet.astro';

A textbook layout: an external grid feeding a step-down transformer
between two voltage levels, then a downstream bus with a load. The three
buses are drawn as short vertical bars (the canonical "node" depiction
in single-line diagrams), with their numbers as labels above.

<Snippet name="recipe-radial-feeder" />
