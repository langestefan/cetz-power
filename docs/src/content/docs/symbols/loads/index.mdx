---
title: Loads
description: Energy-consumer symbols — the generic load arrow, with optional elbow routing for off-axis placement.
---

The loads family covers symbols that consume energy at a connection
point: the generic load (a downward-pointing arrow) and the factory
and EV-charger pictograms for industrial consumers and charging
stations, all with an optional elbow for L-shaped lead routing.

## What's in this family

| Symbol                                   | Role in a one-line diagram                                                                                                   |
| ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| [Load](/symbols/loads/load/)             | Downward-pointing arrow representing a generic consumer. Place at any anchor; pass `elbow:` to route the lead as an L-shape. |
| [Factory](/symbols/loads/factory/)       | Building pictogram (sawtooth roof, steam pipe, window dashes) in a device box, marking an industrial consumer.               |
| [EV charger](/symbols/loads/ev-charger/) | Charging-station pictogram — pedestal, side-view car, or both with the cable (`kind:`), in a device box.                     |

## Source layout

The symbol sources live under `src/symbols/loads/`. More specific
load types (impedance loads, motor-load combos that don't use the
machine symbol, etc.) would belong here.
