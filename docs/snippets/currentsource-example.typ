#import "/src/lib.typ": *
#set page(margin: 4pt, width: auto, height: auto)

#diagram(length: 1.2cm, {
  // Default DC current source: arrow points from `in` to `out`.
  currentsource("i", (0, 0), (2, 0))
})
