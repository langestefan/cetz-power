#import "/src/lib.typ": *
#set page(margin: 4pt, width: auto, height: auto)

#diagram(length: 1.2cm, {
  // Default single-cell battery: long (positive) plate on top, short
  // (negative) plate below, a lead at each end.
  battery("b", (0, 0))
})
