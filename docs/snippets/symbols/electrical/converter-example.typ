#import "/src/lib.typ": *
#set page(margin: 4pt, width: auto, height: auto)

#diagram(length: 1.2cm, {
  // Default: AC→DC (rectifier) — sine in the upper-left triangle,
  // the IEC DC mark in the lower-right one.
  converter("c", (0, 0))
})
