#import "/src/lib.typ": *
#set page(margin: 4pt, width: auto, height: auto)

#diagram(length: 1.2cm, {
  // Default: DC source with + and − markers, two-node placement.
  voltagesource("v", (0, 0), (2, 0))
})
